START TRANSACTION;

SELECT * FROM Products;

UPDATE Products
SET Price=Price*2

ROLLBACK;

START TRANSACTION;

DELETE FROM Products
WHERE ProductID=2;

COMMIT;
SELECT * FROM Products;

START TRANSACTION;

INSERT INTO Products (ProductID)
VALUES(80);

COMMIT;
